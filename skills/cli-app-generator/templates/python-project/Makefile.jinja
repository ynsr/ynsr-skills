CMD ?= .venv/bin/{{ name }}

.PHONY: check
check:
	uv sync -q
	uv run ruff check .
	uv run -- pytest -q tests
	./scripts/verify-cli $(CMD) "list widgets"{% if profiles %} "profile remove demo"{% endif %}
